'''Created on May 23, 2019@author: ashto'''from _collections import defaultdictimport Tokenizerclass InvertedIndex:    def __init__(self):        self.tt = Tokenizer.Tokenizer()        self.indexWords = set()        self.invertedIndex = defaultdict(list)        def createIndexWords(self, folderNum, numFiles):        collections = defaultdict(defaultdict)        for i in range(numFiles):            tokens = self.tt.generateTokenDict(folderNum,i)            collections[i] = tokens            for i in tokens.keys():                self.indexWords.add(i)        return self.indexWords , collections         def generateIDF(self, docNums):        pass            if __name__ == "__main__":    II = InvertedIndex().createIndexWords(0, 3)        print("FIN")         